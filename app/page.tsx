import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Player de repetição de vídeos",
  description: "Cole uma URL, escolha quantas vezes repetir e toque uma sessão focada sem precisar criar conta.",
  alternates: { canonical: "/" },
};

export default function HomePage() {
  return null;
}
